# Логика финблока ATAMŪRA — как всё устроено

Документ описывает доменную логику: потоки данных, чтение документов, БДДС, проверку перед
оплатой (Шерлок + Баффет), Adata и связку с Кроносом. Технический запуск — в `DEPLOY.md`.

---

## 1. Потоки данных (три источника)

```
1С (OData, ~41 база дочек)  ──push /api/ingest──►  ядро SQLite (flow: платежи)
Bitrix (воронка 178, заявки) ──sync_bitrix────────►  ядро SQLite (zayavka: № · объект · поставщик)
Документы заявки (PDF/скан)  ──read (Claude API)──►  ядро SQLite (nakopitel: договор/счёт/АВР)
Adata (по БИН)               ──fetch (2 шага)─────►  ядро SQLite (adata_cache / adata_sb)
```

- **flow** — платежи 1С (`kind` in/out, `supplier`, `amount`, `purpose`, `bin`, `date`). Окно ~3 мес.
- **zayavka** — ВСЕ заявки Bitrix (≈15 900) с названием и **объектом из enum-поля** `ufCrm4_1643707871`.
- **nakopitel** — условия из прочитанных документов: `contract_no, total, avans, retention, barter, article, vypolneno, object, account, bin`.
- **smeta** — бюджет по объектам (импорт из ВДЦ/ППЗ): `object × статья × budget × помесячный план`.

Ключевой принцип: **факты, а не документы**. Ядро хранит факты, дашборд — экспорт поверх.

---

## 2. Матчинг платёж → заявка → объект → статья

Один платёж 1С нужно привязать к заявке, объекту и виду работ:

1. **Платёж → заявка**: по **№ заявки** в назначении платежа (`_num_from(purpose)`). Покрытие ~68%;
   остальные платежи цитируют № счёта/договора, а не заявки (см. §6 — слабое место).
2. **Заявка → объект**: из **enum-поля Bitrix** «Объект эксплуатации» (точно), иначе парсинг названия
   (`_object_from`). Все варианты имени («ЖК «Атмосфера»», «МЖК Керуен») канонизируются в один объект.
3. **Заявка → статья (вид работ)**: из прочитанного договора/тех.требования (ИИ) → канон 13-справочника
   (`_canon_article`: Монолит/Фасад/Кровля/…), иначе из названия заявки.

Функция `bdds_data.attr(purpose)` делает это для КАЖДОГО платежа, чтобы покрыть все оплаты (даже без
прочитанного договора).

---

## 3. Чтение документов (движок накопителя)

`read_zayavka_docs(num)` — единый движок. Ключевые решения:

- **По содержимому, а не по полю.** Сотрудники кладут файлы не в то поле (АВР в поле «Счёт»).
  Поэтому тип документа определяется по **имени файла** (`classify_doc`), затем по ярлыку поля.
- **Комбо-файлы.** Один PDF = «АВР+КС-2+КС-3+счёт» — читается одним проходом полной схемой,
  вычерпывается всё сразу (статья, сумма договора, сумма счёта, **выполнено по АВР**, аванс/удержание/бартер, БИН).
- **Дедуп.** Один файл, залитый в несколько полей, читаем один раз.
- **Устойчивость.** Ретрай транзиентных ошибок (529/429/5xx), 413 → чтение по одному файлу,
  крупный PDF → первые 40 стр, webp/heic → jpg.
- **Не пишем пустышку.** Если ни статьи, ни суммы, ни выполнено — в базу не сохраняем (честный ✕).

Запуск: вкладка **«Обработка»** (кнопка, пул из 6 воркеров, `sonnet-5` + low effort — быстро/дёшево),
CLI `tools/docs_batch.py`, или точечно `tools/read_one.py <№>`.

---

## 4. БДДС по объектам (4 ноги)

Отдельная вкладка. По каждому объекту → статье четыре слоя:

| Нога | Источник | Смысл |
|------|----------|-------|
| **Бюджет** | смета (ВДЦ/ППЗ) | сколько заложено |
| **Договоры** | накопитель (`total`) | сколько законтрактовано |
| **Выполнено** | накопитель (`vypolneno` = АВР/КС) | сколько принято актами |
| **Оплачено** | 1С (`flow`) | сколько заплачено деньгами |

Остаток = Бюджет/Договоры − Оплачено. Клик по объекту → **окно-разбор** (пончик «из чего состоит» +
статьи). Клик по статье → **заявки этого вида работ** (`bdds_article`) → карточка заявки → создать
накопитель / открыть в Bitrix. Клик по дню в графике «Обзора» → все платежи за сутки (`day_payments`).

---

## 5. Проверка перед оплатой: ШЕРЛОК + БАФФЕТ

Модуль `precheck.py`. Срабатывает на заявках стадии **«Оплата»** (воронка 178). Формирует ОДИН
комментарий «Проверка перед оплатой» в карточку Bitrix (идемпотентно по хэшу).

### Разделение ролей — КОГДА что светит

- **ШЕРЛОК = «не платим ли ДВАЖДЫ?»** (ось: платёж ↔ заявка ↔ история 1С).
  Светит, когда: по заявке уже прошли оплаты; тот же БИН+сумма встречались в истории; заявка не
  сматчена ни с оплатой, ни с договором. Вопрос — **задвоение**.

- **БАФФЕТ = «не платим ли ЛИШНЕЕ / за невыполненное?»** (ось: договор ↔ выполнено ↔ оплачено).
  Светит, когда: сумма к оплате > остатка договора; **оплата > выполнено (АВР)** — платим за
  непринятые работы; не учтено гар.удержание; не учтён бартер; счёт без КС-актов. Вопрос — **переплата**.

### Логика Баффета (после фикса)

```
остаток = договор − гар.удержание − бартер − уже оплачено (1С)
```
(аванс НЕ вычитаем отдельно — он уже входит в оплаты 1С). Красный флаг «оплата+уже оплачено > выполнено»
— главная защита от оплаты невыполненного.

### Итог вердикта (3 уровня)

- ⛔ **СТОП** — есть 🔴 (у Шерлока или Баффета),
- ⚠️ **есть замечания**,
- ✅ **можно к оплате** — только когда реально чисто.

### Контрагент (Adata) — падает в тот же комментарий

Красные флаги благонадёжности (аресты, фиктивные сделки, налог.долг, суды, розыск/терроризм
руководителя, ЭСФ) из модулей Adata (§7) добавляются в замечания. Кэш `adata_sb` по БИН.

---

## 6. Слабое место матчинга (ВАЖНО — задвоения матчатся неверно)

Шерлок матчит платёж→заявку по **№ заявки в назначении 1С**. Два типа ошибок:

- **Ложный пропуск** (реальный дубль НЕ виден): платёж без № заявки (ссылается на № счёта) не
  привязывается → прошлую оплату Шерлок не видит → пропустит настоящее задвоение.
- **Ложное срабатывание** (обычный повтор принят за дубль): запасная проверка «тот же БИН+сумма ≥2
  раз» помечает как дубль **регулярные** платежи (аренда крана 360 000 ₸ дважды в месяц — это НЕ дубль).

**Как чинить** (план): матчить по нескольким ключам — № заявки (осн.) + **№ счёта/акта** (из накопителя) +
близость дат; отличать **дубль** (тот же счёт / тот же БИН+сумма в узком окне) от **регулярной услуги**
(повтор растянут по периодам). Это «вторая нога матча».

---

## 7. Adata — проверка контрагента (модули благонадёжности)

Двухшаговый протокол: **Get Request** (свой slug на модуль) → request-токен → **Check Request**
(общий `company/info/check`, поллинг) → data. Санкции — особый (по названию, одношаговый).

Подключённые модули (`adata.MODULES`): `info, basic, status, riskfactor, trustworthy-extended,
trustworthy-plus, rehab-bankruptcy, risk-check, courtcase, license, licenses-certificates, tax-mode,
tax, tax-deduction/dynamics, tax-debt/details`. `sb_card(bin)` собирает из них флаги СБ.

Проверка привязана к 10-пунктовому чек-листу контрагента (цена/ОКЭД/лицензия/сотрудники/режим
налогов/лимиты/наименование/дочка/**благонадёжность**/сроки). ~6 из 15 закрыты Adata, остальное — из
нашего договора/заявки (удержание/бартер/КС/выполнено — Баффет).

Ручная проверка: `tools/check_bin.py <БИН> [--sb|--full]`. В панели — кнопка «Проверить контрагента».

---

## 8. Связка с Кроносом (задачи участникам процесса) — план

Кронос (`@cronos_murabot`, Telegram + Mini App, роли) становится **оболочкой задач ERP**. Заявка
Bitrix идёт по стадиям, каждая стадия → роль:

```
Создана → Согласование → ЮР-проверка → СБ-проверка → ОПЛАТА → Закрыто
                              │            │            │
                          юристы        СБ         фин.дир/бухгалтерия
```

Когда заявка входит в стадию, ответственная роль получает **задачу** во вкладке «Задачи» Кроноса
(№ заявки, что проверить, ссылка в Bitrix, кнопки принять/вернуть → двигают стадию). Источник задач —
стадии Bitrix (финблок уже умеет `items_in_stages`); финблок отдаёт задачи по роли, Кронос показывает.

Детали интеграции и промт для реализации — в отдельном запросе к репозиторию Кроноса.

---

## Карта кода (кратко)

- `parser_1c_odata.py` — парсер 1С (на 1С-сервере), пушит срез на `/api/ingest`.
- `server.py` — ядро + SPA: `sync_bitrix`, `bdds_data`, `read_zayavka_docs`, `queue_*`,
  `day_payments`, `adata_check`, `bdds_article` + HTTP-роуты + вкладки.
- `bx_reader.py` — Bitrix REST, скачивание/чтение документов (Claude API), схемы, `classify_doc`, `object_enum`.
- `adata.py` — клиент Adata (модули, `fetch`, `fetch_modules`, `fetch_sanctions`, `sb_card`, рендер).
- `precheck.py` — Шерлок + Баффет + флаги контрагента, комментарий в Bitrix.
- `tools/` — `sync_bitrix, docs_batch, read_one, coverage, check_bin, oplata_audit, smeta_import, precheck_run`.
